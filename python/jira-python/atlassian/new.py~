from jira.client import JIRA 

options = {'server': 'https://jira.atlassian.com'}
jira = JIRA(options)
#jira = JIRA(basic_auth=('anto.daniel@gmail.com','1ds01is005'))
#jira = JIRA(options = jira_options, basic_auth = ('anto.daniel@gmail.com','1ds01is005'))
projects = jira.projects()
keys = sorted([project.key for project in projects])[2:5]
issue = jira.issue('TST-53430')
import re
#atl_comments = [comment for comment in issue.fields.comment.comments
#                                if re.search(r'@atlassian.com$', comment.author.emailAddress)]
#for comment in issue.fields.comment.description:
#    print comment
print issue.fields.comment.name
#props = jira.application_properties()
#issues = jira.search_issues('assignee=anto.daniel')
#from collections import Counter
#top_three = Counter([issue.fields.project.key for issue in issues]).most_common(3)

#new_issue = jira.create_issue(project={'key': 'PROJ'}, summary='New issue from jira-python',description='Look into this one', issuetype={'name': 'New Issue Bug'})
